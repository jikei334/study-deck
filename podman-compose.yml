version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - DB_PATH=/app/data/study-deck.db
      - PORT=3000
      - CORS_ORIGIN=http://localhost:5173
